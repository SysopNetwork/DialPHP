# Build artifacts
/Build/
/build_output.txt
/build_output_release.txt

# Development-only directories (not for public distribution)
/Marks Notes/
/Demo BBS Files/
/OLD PHP/
/WG 3.3 SDK/
/Original Source Code/

# Claude Code project files
/.claude/
/CLAUDE.md

# Local working PHP Examples (credentials present — use PHP CODE EXAMPLES/ for the clean version)
/PHP Examples/

# Local test deployment — not for public distribution
/dp.bbs.lat/

# Runtime data files generated by the PHP client at run time
**/data/login_attempts.json
**/data/stats_cache.json

# Release zip artifacts
*.zip

# Windows Explorer artifacts
Thumbs.db
Desktop.ini
ehthumbs.db

# Visual Studio artifacts (pdb/lib/exp go to Build/ which is already excluded)
*.user
